# PM Case Showcase

独立的 AI 产品案例展示页，用来说明 Truth Finder 如何把新闻报道转化为可玩的调查 pipeline。

## Commands

- `npm install`
- `npm run dev`
- `npm test`
- `npm run build`

默认开发地址是 [http://localhost:4173](http://localhost:4173)。
